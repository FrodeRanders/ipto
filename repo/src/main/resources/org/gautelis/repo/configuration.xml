<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

    <!--
        Corresponds to class org.gautelis.vopn.db.Database.Configuration
    -->
    <entry key="url">
        jdbc:postgresql://localhost:5432/repo
    </entry>

    <!--
        Corresponds to class org.gautelis.vopn.db.Database.Configuration
    -->
    <entry key="user">
        repo
    </entry>

    <!--
        Corresponds to class org.gautelis.vopn.db.Database.Configuration
    -->
    <entry key="password">
        repo
    </entry>

    <!--
        Corresponds to class org.gautelis.repo.model.Configuration
    -->
    <entry key="repository.database.adapter">
        org.gautelis.repo.search.query.postgresql.PostgresAdapter
    </entry>

    <!--
        Corresponds to class org.gautelis.repo.model.Configuration
    -->
    <entry key="repository.events.threshold">
        60
    </entry>

    <!--
        Corresponds to class org.gautelis.repo.model.Configuration
    -->
    <entry key="repository.events.listeners">
        org.gautelis.repo.listeners.NopActionListener
    </entry>

    <!--
        Corresponds to class org.gautelis.repo.model.Configuration
    -->
    <entry key="repository.cache.look_behind">
        true
    </entry>

    <!--
        Corresponds to class org.gautelis.repo.model.Configuration
    -->
    <entry key="repository.cache.max_size">
        1000
    </entry>

    <!--
        Corresponds to class org.gautelis.repo.model.Configuration
    -->
    <entry key="repository.cache.idle_check_interval">
        60
    </entry>

    <!--
        Unused, since we have a 'url'.
    -->
    <entry key="driver" />
    <entry key="manager" />
    <entry key="server" />
    <entry key="port" />
    <entry key="database" />
</properties>



