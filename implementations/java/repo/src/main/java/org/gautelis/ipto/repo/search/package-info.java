/**
 * Search entry points and result types for repository queries.
 * <p>
 * The package contains the high-level search request object used by the
 * repository API together with the result wrapper returned from searches. The
 * underlying predicate model and SQL-generation machinery live in the
 * {@code model} and {@code query} subpackages.
 */
package org.gautelis.ipto.repo.search;
