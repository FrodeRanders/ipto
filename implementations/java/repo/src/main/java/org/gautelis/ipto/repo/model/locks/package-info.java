/**
 * Lock model for coordinating writes and administrative operations on units.
 * <p>
 * The package provides lock value objects and lock-type definitions used from
 * the repository object model when units are explicitly locked or unlocked.
 */
package org.gautelis.ipto.repo.model.locks;
